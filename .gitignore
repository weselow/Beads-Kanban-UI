# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# tauri
/src-tauri/target/

# rust server
/server/target/

# mcp config (user-specific)
.mcp.json

# beads orchestration (user-specific)
.beads/
AGENTS.md
CLAUDE.md

# Stray bd auto-export files (bd v1.0.2 CWD bug — see beads-web-b9c)
/issues.jsonl

# spec and test files
SPEC.md
test-*.md
test-*.txt
.verification_logs/

# Git worktrees
.worktrees/
Vent

# Claude Code project config (user-specific)
.claude/

# IDE/tool artifacts
.history/
.playwright-mcp/
agentform/

# Extra build targets
/server/target2/

# Dolt server runtime files
.dolt/
.doltcfg/
.dolt_dropped_databases/
config.yaml
beads_*/

# Compiled binaries
beads-web-linux-*
beads-web-win-*
beads-web-darwin-*

# Beads orchestration
.claude/.upgrades/

# package managers other than npm
# The build uses npm everywhere (release.yml, quality.yml, flake.nix via
# npmDepsHash). A stray pnpm run rewrites node_modules into the pnpm layout
# and moves the npm-installed packages into node_modules/.ignored.
/pnpm-lock.yaml
/pnpm-workspace.yaml
/yarn.lock
/bun.lockb
